# Copyright (c) 2026 Astrolune contributors
# SPDX-License-Identifier: MIT

[package]
name = "storage"
description = "Validator-local persistent chain storage for AstroLune"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
codec = { version = "0.1.0", path = "../codec" }
crypto = { version = "0.1.0", path = "../crypto" }
transaction = { version = "0.1.0", path = "../transaction" }
state = { version = "0.1.0", path = "../state" }
types = { version = "0.1.0", path = "../types" }

[lints]
workspace = true
